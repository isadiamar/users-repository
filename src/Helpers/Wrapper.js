
//Wrapper Component
const Wrapper = (props) =>{
    //The children props holds all the content you are passing between the opening
    //and closing tags
    return props.children;
}

//Export
export default Wrapper;